# Baseline-Import Lifecycle PDF Export

`main.pdf` is the one-page wide-format export of `../baseline-import-lifecycle.mmd` rendered through `../baseline-import-lifecycle.png`.

Validation completed on 2026-08-25: strict Typst compilation passed without warnings; deterministic PDF verification passed; page count is one; and visual review confirmed the title, complete sequence diagram, and editable-source note appear on the single page.

The Mermaid source remains the editable, source-controlled representation. The PDF is a convenient review/export artifact; it does not replace the Mermaid source.
